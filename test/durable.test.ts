import { describe, jest } from "@jest/globals";

jest.setTimeout(10000);

describe("Durability tests", () => {
  // TODO: write durability tests that take into account the caching layer
  // for non durable functions
});
