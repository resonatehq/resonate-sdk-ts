// @resonatehq/connector-pg — a dual-role Resonate connection (Network +
// Source) whose server is Postgres (see github.com/resonatehq/resonate-pg).

export { PostgresConnection, type PostgresConnectionConfig } from "./postgres.js";
